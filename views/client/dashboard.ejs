<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mis Propuestas - MICE Catering</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>
<body class="bg-gradient-to-br from-slate-50 to-slate-100" style="font-family: 'Inter', sans-serif;">

<!-- Header Elegante Cliente -->
<header class="bg-white border-b border-slate-200 shadow-sm sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
        <div class="flex items-center gap-3">
            <div class="w-12 h-12 bg-gradient-to-br from-blue-600 to-blue-700 rounded-lg flex items-center justify-center">
                <span class="text-white text-xl font-bold">🍽️</span>
            </div>
            <div>
                <h1 class="text-xl font-bold text-gray-900">MICE Catering</h1>
                <p class="text-xs text-gray-500">Propuestas de Eventos</p>
            </div>
        </div>
        <div class="flex items-center gap-4">
            <button class="text-gray-600 hover:text-gray-900 print:hidden" onclick="window.print()">
                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20"><path d="M5 4a2 2 0 012-2h6a2 2 0 012 2v14l-5-2.5L5 18V4z"></path></svg>
            </button>
            <button class="px-4 py-2 text-gray-600 hover:text-gray-900 text-sm print:hidden" onclick="location.href='/logout'">
                Cerrar sesión
            </button>
        </div>
    </div>
</header>

<main class="max-w-7xl mx-auto px-6 py-12">
    
    <!-- Bienvenida -->
    <div class="mb-12">
        <h2 class="text-4xl font-bold text-gray-900 mb-2">Bienvenido a tu Centro de Propuestas</h2>
        <p class="text-lg text-gray-600">Visualiza, revisa y gestiona todas tus propuestas de eventos</p>
    </div>

    <!-- Stats -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-12">
        <div class="bg-white rounded-lg border border-slate-200 p-6 hover:shadow-lg transition">
            <div class="text-3xl font-bold text-blue-600 mb-1"><%= totalProposals || 0 %></div>
            <div class="text-sm text-gray-600">Propuestas Totales</div>
            <div class="mt-3 text-xs text-gray-500 bg-blue-50 px-2 py-1 rounded w-fit">
                <% if (totalProposals > 0) { %>
                    Última: hace 3 días
                <% } else { %>
                    Sin propuestas aún
                <% } %>
            </div>
        </div>

        <div class="bg-white rounded-lg border border-slate-200 p-6 hover:shadow-lg transition">
            <div class="text-3xl font-bold text-green-600 mb-1"><%= activeProposals || 0 %></div>
            <div class="text-sm text-gray-600">Propuestas Activas</div>
            <div class="mt-3 text-xs text-gray-500 bg-green-50 px-2 py-1 rounded w-fit">
                Válidas hasta responder
            </div>
        </div>

        <div class="bg-white rounded-lg border border-slate-200 p-6 hover:shadow-lg transition">
            <div class="text-3xl font-bold text-purple-600 mb-1"><%= acceptedProposals || 0 %></div>
            <div class="text-sm text-gray-600">Propuestas Aceptadas</div>
            <div class="mt-3 text-xs text-gray-500 bg-purple-50 px-2 py-1 rounded w-fit">
                Confirmadas
            </div>
        </div>

        <div class="bg-white rounded-lg border border-slate-200 p-6 hover:shadow-lg transition">
            <div class="text-3xl font-bold text-orange-600 mb-1"><%= totalAmount || '0€' %></div>
            <div class="text-sm text-gray-600">Presupuesto Total</div>
            <div class="mt-3 text-xs text-gray-500 bg-orange-50 px-2 py-1 rounded w-fit">
                De propuestas
            </div>
        </div>
    </div>

    <!-- Sección de Propuestas -->
    <div>
        <div class="mb-6 flex items-center justify-between">
            <h3 class="text-2xl font-bold text-gray-900 flex items-center gap-2">
                <span>📋</span> Tus Propuestas
            </h3>
            <span class="text-sm text-gray-500"><%= totalProposals %> propuesta(s)</span>
        </div>

        <% if (proposals && proposals.length > 0) { %>
            <div class="space-y-4">
                <% proposals.forEach(proposal => { %>
                    <div class="bg-white rounded-lg border border-slate-200 overflow-hidden hover:shadow-xl hover:border-blue-300 transition">
                        <!-- Encabezado de Propuesta -->
                        <div class="bg-gradient-to-r from-slate-50 to-blue-50 border-b border-slate-200 px-6 py-4">
                            <div class="flex items-start justify-between">
                                <div class="flex-1">
                                    <div class="flex items-center gap-3 mb-2">
                                        <h4 class="text-lg font-bold text-gray-900">
                                            Propuesta #<%= proposal.id %>
                                        </h4>
                                        <span class="px-3 py-1 text-xs font-semibold rounded-full <%= 
                                            proposal.status === 'accepted' ? 'bg-green-100 text-green-700' :
                                            proposal.status === 'sent' ? 'bg-blue-100 text-blue-700' :
                                            'bg-gray-100 text-gray-700'
                                        %>">
                                            <%= proposal.status === 'accepted' ? '✓ Aceptada' : 
                                                proposal.status === 'sent' ? '📧 Enviada' : 
                                                '📝 Borrador' %>
                                        </span>
                                    </div>
                                    <p class="text-sm text-gray-600"><strong><%= proposal.client_name %></strong></p>
                                </div>
                                <div class="text-right">
                                    <div class="text-2xl font-bold text-gray-900"><%= formatCurrency(proposal.total_amount || 0) %></div>
                                    <div class="text-xs text-gray-500 mt-1">Presupuesto Total</div>
                                </div>
                            </div>
                        </div>

                        <!-- Contenido de Propuesta -->
                        <div class="px-6 py-4">
                            <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-4">
                                <div class="bg-slate-50 rounded p-3">
                                    <div class="text-xs text-gray-600 uppercase font-semibold">Evento</div>
                                    <div class="text-sm font-bold text-gray-900 mt-1">
                                        <%= new Date(proposal.event_date).toLocaleDateString('es-ES', {
                                            year: 'numeric',
                                            month: 'long',
                                            day: 'numeric'
                                        }) %>
                                    </div>
                                </div>
                                <div class="bg-slate-50 rounded p-3">
                                    <div class="text-xs text-gray-600 uppercase font-semibold">Comensales</div>
                                    <div class="text-sm font-bold text-gray-900 mt-1"><%= proposal.pax %> PAX</div>
                                </div>
                                <div class="bg-slate-50 rounded p-3">
                                    <div class="text-xs text-gray-600 uppercase font-semibold">Venue</div>
                                    <div class="text-sm font-bold text-gray-900 mt-1">
                                        <% if (proposal.venues && proposal.venues.length > 0) { %>
                                            <%= proposal.venues[0].name %>
                                        <% } else { %>
                                            -
                                        <% } %>
                                    </div>
                                </div>
                                <div class="bg-slate-50 rounded p-3">
                                    <div class="text-xs text-gray-600 uppercase font-semibold">Válida Hasta</div>
                                    <div class="text-sm font-bold text-gray-900 mt-1">
                                        <% 
                                            const validUntil = new Date(proposal.valid_until);
                                            const today = new Date();
                                            const daysLeft = Math.ceil((validUntil - today) / (1000 * 60 * 60 * 24));
                                        %>
                                        <% if (daysLeft > 0) { %>
                                            <%= daysLeft %> días
                                        <% } else { %>
                                            Vencida
                                        <% } %>
                                    </div>
                                </div>
                            </div>

                            <!-- Detalles de Propuesta -->
                            <div class="border-t border-slate-200 pt-4 mt-4">
                                <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                                    <!-- Venues -->
                                    <div>
                                        <h5 class="text-xs font-bold text-gray-700 uppercase mb-2">🏢 Venue</h5>
                                        <% if (proposal.venues && proposal.venues.length > 0) { %>
                                            <% proposal.venues.slice(0, 2).forEach(venue => { %>
                                                <div class="text-sm text-gray-600 mb-1"><%= venue.name %></div>
                                            <% }); %>
                                        <% } else { %>
                                            <div class="text-sm text-gray-500 italic">Sin venue asignado</div>
                                        <% } %>
                                    </div>

                                    <!-- Servicios -->
                                    <div>
                                        <h5 class="text-xs font-bold text-gray-700 uppercase mb-2">🍽️ Servicios</h5>
                                        <% if (proposal.services && proposal.services.length > 0) { %>
                                            <% proposal.services.slice(0, 3).forEach(service => { %>
                                                <div class="text-sm text-gray-600 mb-1">
                                                    • <%= service.title || 'Servicio sin nombre' %>
                                                </div>
                                            <% }); %>
                                            <% if (proposal.services.length > 3) { %>
                                                <div class="text-xs text-gray-500 mt-1">+<%= proposal.services.length - 3 %> más</div>
                                            <% } %>
                                        <% } else { %>
                                            <div class="text-sm text-gray-500 italic">Sin servicios</div>
                                        <% } %>
                                    </div>

                                    <!-- Acciones -->
                                    <div class="flex flex-col gap-2">
                                        <a href="/p/<%= proposal.hash %>" class="px-4 py-2 bg-blue-600 text-white rounded font-medium hover:bg-blue-700 transition text-center text-sm">
                                            Ver Propuesta Completa
                                        </a>
                                        <button onclick="window.open('/p/<%= proposal.hash %>', '_blank')" class="px-4 py-2 bg-slate-200 text-gray-700 rounded font-medium hover:bg-slate-300 transition text-sm print:hidden">
                                            Abrir en Nueva Pestaña
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Footer -->
                        <div class="bg-slate-50 border-t border-slate-200 px-6 py-3 flex items-center justify-between text-xs text-gray-600">
                            <span>Creada: <%= new Date(proposal.created_at).toLocaleDateString('es-ES') %></span>
                            <span>ID: <%= proposal.id %></span>
                        </div>
                    </div>
                <% }); %>
            </div>

        <% } else { %>
            <!-- Empty State -->
            <div class="bg-white rounded-lg border border-slate-200 p-12 text-center">
                <div class="text-4xl mb-4">📭</div>
                <h4 class="text-xl font-semibold text-gray-900 mb-2">No tienes propuestas</h4>
                <p class="text-gray-600 mb-6">Próximamente recibirás propuestas personalizadas para tus eventos</p>
                <button onclick="location.href='/'" class="px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition font-medium">
                    Volver al Inicio
                </button>
            </div>
        <% } %>
    </div>

    <!-- Footer Info -->
    <div class="mt-12 bg-white rounded-lg border border-slate-200 p-6">
        <h4 class="font-semibold text-gray-900 mb-3 flex items-center gap-2">
            <span>ℹ️</span> Cómo trabajamos
        </h4>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 text-sm text-gray-600">
            <div>
                <div class="font-semibold text-gray-900 mb-1">1. Propuesta</div>
                <p>Recibio tu propuesta personalizada según tus necesidades y presupuesto</p>
            </div>
            <div>
                <div class="font-semibold text-gray-900 mb-1">2. Revisión</div>
                <p>Revisa los detalles, servicios, precios y contactanos con tus dudas</p>
            </div>
            <div>
                <div class="font-semibold text-gray-900 mb-1">3. Confirmación</div>
                <p>Acepta la propuesta y confirma tu evento con los mejores servicios</p>
            </div>
        </div>
    </div>

</main>

<!-- Footer -->
<footer class="bg-slate-900 text-white mt-16 py-8 print:hidden">
    <div class="max-w-7xl mx-auto px-6 text-center text-sm text-slate-400">
        <p>© 2026 MICE Catering Proposals - Plataforma de Gestión de Eventos</p>
    </div>
</footer>

<script>
    // Helpers
    function formatCurrency(amount) {
        if (!amount) return '0,00 €';
        return amount.toLocaleString('es-ES', {
            minimumFractionDigits: 2,
            maximumFractionDigits: 2
        }) + ' €';
    }

    // Print styles
    /* Use a <style> tag for CSS, not inside <script> */
</script>

<style>
    @media print {
        body { background: white; }
        header, footer, .print\:hidden, button { display: none !important; }
        .bg-gradient-to-r { background: white !important; }
    }
</style>

</body>
</html>
