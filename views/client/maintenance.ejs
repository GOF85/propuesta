<%- include('../partials/header-client') %>

<main class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 flex items-center justify-center py-8">
  <div class="max-w-md w-full mx-4 bg-white rounded-lg shadow-lg p-8 text-center">

    <!-- Icono -->
    <div class="text-6xl mb-6">🔄</div>

    <!-- Título -->
    <h1 class="text-3xl font-bold text-gray-900 mb-3">
      Propuesta en Revisión
    </h1>

    <!-- Descripción -->
    <p class="text-gray-600 mb-8 leading-relaxed">
      El comercial está realizando cambios en tu propuesta.
    </p>

    <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-8">
      <p class="text-sm text-blue-900">
        📧 Te enviaremos un email cuando esté lista la nueva versión.
      </p>
    </div>

    <!-- Info de la propuesta -->
    <div class="bg-gray-50 rounded-lg p-6 mb-8">
      <p class="text-xs text-gray-600 mb-2">PROPUESTA</p>
      <p class="text-2xl font-bold text-gray-900">#<%= proposal.id %></p>
      <p class="text-sm text-gray-600 mt-3"><%= proposal.client_name %></p>
    </div>

    <!-- Indicador de progreso -->
    <div class="mb-8">
      <div class="w-full bg-gray-200 rounded-full h-2">
        <div class="bg-blue-600 h-2 rounded-full" style="width: 60%; animation: pulse 2s infinite;"></div>
      </div>
    </div>

    <!-- Botón -->
    <button onclick="location.reload()" class="btn btn-primary w-full mb-4">
      🔄 Verificar Cambios
    </button>

    <!-- Info de contacto -->
    <p class="text-xs text-gray-500 mt-6">
      Si tienes urgencia, contáctanos directamente por email
    </p>
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm0-2a6 6 0 100-12 6 6 0 000 12z" clip-rule="evenodd"></path>
                    </svg>
                </div>
                <h2 class="text-xl font-semibold text-gray-900 mb-2">Propuesta en edición</h2>
                <p class="text-gray-600">El comercial está personalizando tu propuesta. Te avisaremos cuando esté lista para revisar.</p>
            </div>
            
            <!-- Loading Indicator -->
            <div class="flex items-center justify-center gap-1 my-6">
                <div class="w-2 h-2 rounded-full bg-blue-600 animate-bounce" style="animation-delay: 0s"></div>
                <div class="w-2 h-2 rounded-full bg-blue-600 animate-bounce" style="animation-delay: 0.2s"></div>
                <div class="w-2 h-2 rounded-full bg-blue-600 animate-bounce" style="animation-delay: 0.4s"></div>
            </div>
        </div>
        
        <!-- Information -->
        <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-6">
            <p class="text-sm text-blue-900">
                Esta página se actualizará automáticamente cuando la propuesta esté lista. 
                <br><br>
                <strong>No cierres esta pestaña.</strong>
            </p>
        </div>
        
        <!-- Refresh Script -->
        <script>
            // Recargar la página cada 5 segundos
            setInterval(() => {
                location.reload();
            }, 5000);
        </script>
        
        <!-- Manual Refresh Button -->
        <button 
            onclick="location.reload()" 
            class="text-blue-600 hover:text-blue-700 font-medium text-sm"
        >
            Actualizar ahora
        </button>
    </div>
</body>
</html>
